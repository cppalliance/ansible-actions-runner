---
collections:
  # Only needed when targeting Windows hosts.
  - name: ansible.windows
    version: ">=2.0.0"
  # Only needed on Windows hosts, and only when something there sets
  # serial_execution: win_lineinfile writes the hook lines into .env.
  #
  # chocolatey.chocolatey is deliberately absent. The role installs Chocolatey
  # packages by calling choco directly — tasks/windows.yml says why.
  - name: community.windows
